# سند جامع معماری سامانه پس از بازطراحی و بازآفرینی بنیادین (ARCHITECTURE-AFTER-REFACTOR)
> **سامانه:** پلتفرم استراتژی برند و تدوین سند هویت کسب‌وکار «دیجیتال مارکت» (DIGITAL MARKET)  
> **محیط اجرایی:** Single Page Application (React 18 + Vite + Tailwind CSS)  
> **مخاطب هدف:** اکوسیستم کسب‌وکار ایران (۷۵۳ صنف در ۳۱ صنعت کلان با انطباق کامل بر کدهای نظام صنفی و ISIC)  
> **وضعیت نهایی:** ممیزی‌شده، بدون تقلب تستی (Zero Test Cheats)، تاییدشده در محیط پروداکشن  

---

## ۱. نمای کلی و اصول بنیادین معماری جدید

پس از اجرای کامل فازهای بازطراحی، معماری سامانه از یک ساختار گفتگومحور با چالش‌های نشت واژگان، داده‌های ساختگی و وابستگی‌های مبهم، به یک **موتور برنامه‌ریزی راهبردی سازمانی (Executive Brand Strategy Workspace)** تبدیل شده است. این دگرگونی بر ۵ اصل تخطی‌ناپذیر استوار است:

```
┌────────────────────────────────────────────────────────────────────────────────┐
│                       DIGITAL MARKET WORKSPACE ARCHITECTURE                    │
├────────────────────────────────────────────────────────────────────────────────┤
│  [1. ایزولاسیون کامل حوزه‌ها]       [2. روتر چندبعدی بافتار (۱۵ بعد)]             │
│  753 صنف در 31 صنعت کلان          تفکیک مشتری، کانال، درآمد، مقیاس، جغرافیا... │
├────────────────────────────────────────────────────────────────────────────────┤
│  [3. ماشین وضعیت ۸ مرحله‌ای]       [4. سیستم مدیریت مجهولات و تناقضات]           │
│  Anti-Skip Phase Gates            ردگیری فرضیات نیازمند تست و ابطال فازهای پسین│
├────────────────────────────────────────────────────────────────────────────────┤
│  [5. محیط کاربری غیرچت‌باتی]       [6. امنیت BYOK و ایزولاسیون اعتبارسنجی]      │
│  سیاه و سفید خالص (#000000)       کلید در حافظه موقت سشن، خروج امن داده‌ها      │
└────────────────────────────────────────────────────────────────────────────────┘
```

---

## ۲. مراجع یگانه حقیقت (Single Sources of Truth - SSOT)

در معماری جدید، هیچ داده یا منطقی تکرار نشده و هر بخش متکی بر یک منبع واحد است:

| شناسه منبع | فایل مرجع | مسئولیت و کارکرد |
|---|---|---|
| **SSOT-Taxonomy** | `platform/src/data/businessTaxonomy753.js` | کاتالوگ جامع ۷۵۳ صنف کسب‌وکار در ۳۱ صنعت کلان، کدهای نظام صنفی، کدهای ISIC، کلیدواژه‌ها، آرکی‌تایپ‌ها و اورلی‌های فعال |
| **SSOT-Router** | `platform/src/data/businessContextRouter.js` | طبقه‌بندی هوشمند کسب‌وکار در ۱۵ بعد متعامد، اعمال اورلی‌های مقرراتی و تولید قوانین انطباق فازهای ۱ تا ۸ |
| **SSOT-Vocabulary** | `platform/src/data/industryVocabularyMap.js` | نقشه واژگان مجاز و واژگان ممنوعه/آلاینده در ۳۱ صنعت کلان جهت جلوگیری قطعی از نشت اصطلاحات نامربوط |
| **SSOT-Templates** | `platform/src/data/allPhasesTemplates.js` & `phase2Templates.js` | بانک پرسش‌های راهبردی فازهای ۱ تا ۸ همراه با فرمول ترکیب زنجیره‌ای (Base + Industry + Overlay) |
| **SSOT-Knowledge** | `platform/src/data/wikiKnowledge.js` | پلی‌بوک‌های دانشی ۱۶۵ منبع علمی و متون راهبردی بازار ایران |
| **SSOT-Engine** | `platform/src/services/orchestratorEngine.js` | ماشین پردازش وضعیت، ثبت داده‌های فازها، مدیریت دروازه‌های خروج و صدور اسناد ۵ لایه‌ای |
| **SSOT-Security** | `platform/src/services/endpointSecurity.js` | مدیریت کلیدهای BYOK در SessionStorage، پالایش آدرس‌ها و پاک‌سازی داده‌های حساس |
| **SSOT-Persistence** | `platform/src/services/persistenceManager.js` | ذخیره‌سازی ایزوله در LocalStorage، مهاجرت اسکیمای v0 به v1 و بازیابی خودکار از داده‌های مخدوش |

---

## ۳. سلسله‌مراتب تخصصی‌سازی اصناف (Specialization Hierarchy)

سیستم به هیچ وجه بر پیش‌فرض‌های کلی تکیه نمی‌کند. تخصصی‌سازی سوالات و محتوا بر اساس اولویت ۸ سطحی زیر تعیین می‌شود:

```
[۱. صنف دقیق (BT-xxxx)] ──► [۲. ماژول صنعت کلان (IND-xx)] ──► [۳. آرکی‌تایپ پایه]
                                                                        │
                                                                        ▼
[۶. پاسخ‌های قبلی بنیان‌گذار] ◄── [۵. مرحله بلوغ و جغرافیا] ◄── [۴. محورهای ۱۵گانه بافتار]
         │
         ▼
[۷. حقایق قطعی [FACT]] ──► [۸. تولید پرسش تخصصی و سند تحویل‌شدنی ۵ لایه‌ای]
```

---

## ۴. ماشین وضعیت ۸ مرحله‌ای و دروازه‌های ارزیابی (Phase Gates)

عبور از هر یک از فازهای ۸ گانه منوط به احراز معیارهای سخت‌گیرانه اعتبارسنجی در `phaseGateValidator.js` است:

1. **فاز ۱ (بنیاد کسب‌وکار و اقتصاد واحد):** احراز صنف، مدل درآمدی، جغرافیا، پیشنهاد محوری و پوشش هزینه‌های ثابت.
2. **فاز ۲ (هوش بازار و شناخت مشتری):** شناسایی رقبای محلی، درد مشتریان، مدل قیمت‌گذاری و کانال جذب بدون نشت اصطلاحات شرکتی (CAC/LTV) در اصناف سنتی.
3. **فاز ۳ (جهت‌گیری و استراتژی برند):** تعریف بخش هدف، موضع انحصار (Only-ness)، مرزهای راهبردی ("نه" گفتن) و وعده برند.
4. **فاز ۴ (هویت و شخصیت برند):** تعیین کهن‌الگوی روان‌شناختی، ۳ صفت رفتاری و گاردریل‌های لحن.
5. **فاز ۵ (هویت کلامی و پیام‌رسانی):** قلاب ۳۰ ثانیه‌ای، سبک صدا و لیست واژگان ممنوعه برند.
6. **فاز ۶ (نام‌گذاری و هدایت خلاقانه):** تعیین قلمرو نام‌گذاری، سبک شعار (Tagline) و دستورالعمل دیزاین.
7. **فاز ۷ (سیستم هویت بصری):** پالت رنگی روان‌شناختی، تایپوگرافی فارسی، فرم لوگو و سنجه‌های بصری.
8. **فاز ۸ (برنامه فعال‌سازی اجرایی و اعتبار):** رهبری فکری، روابط عمومی/پادکست، قیف تجاری، پلی‌بوک بحران و نقشه راه ۹۰/۱۸۰/۳۶۵ روزه.

### گراف ابطال وابستگی‌ها (Dependency Invalidation Graph)
اگر کاربری پس از تکمیل فازهای پیشین، به فاز ۱ یا ۳ بازگردد و پاسخی اساسی را تغییر دهد:
- موتور تناقضات (`contradictionEngine.js`) مغایرت‌ها را ثبت می‌کند.
- متد `invalidateDependentPhases` تمامی فازهای پسین را به وضعیت `INVALIDATED` تغییر می‌دهد.
- تا زمانی که دروازه فاز تغییریافته مجدداً تایید نشود، فازهای بعدی قفل خواهند ماند.

---

## ۵. چرخه حیات ۴ مرحله‌ای پرسش‌های هوش مصنوعی (Dynamic Question Lifecycle)

جهت جلوگیری کامل از ایجاد حلقه‌های تکراری یا پرسش‌های نامربوط، تمامی سوالات پویا از چرخه حیات رسمی زیر تبعیت می‌کنند:

```
┌──────────────┐         نمایش در کارت پرسش         ┌─────────────┐
│   PENDING    │ ──────────────────────────────────► │   ACTIVE    │
└──────────────┘                                     └─────────────┘
                                                            │
                                                     پاسخ کاربر
                                                            │
                                                            ▼
┌──────────────┐          بایگانی و عدم تکرار        ┌─────────────┐
│   ARCHIVED   │ ◄────────────────────────────────── │  CONSUMED   │
└──────────────┘                                     └─────────────┘
```

---

## ۶. سیستم مدیریت مجهولات رسمی (Unknowns Architecture)

هیچ حدس و گمانی به عنوان «حقیقت» وارد دوسیه استراتژیک نمی‌شود:
- **دسته‌بندی‌ها:** `FINANCIAL_METRICS`, `MARKET_BENCHMARK`, `OPERATIONAL_TIMING`, `UNCERTAINTY`, `EXPLICIT_IGNORANCE`.
- **درجه اهمیت:**
  - `BLOCKING_UNKNOWN`: مانع از خروج از فاز تا زمان تعیین تکلیف یا پذیرش ریسک.
  - `NON_BLOCKING_UNKNOWN`: ثبت به عنوان فرضیه دوره پایلوت با ماموریت حقیقت‌یابی ۳۰ روزه.
- **برچسب‌گذاری در اسناد:** مجهولات رسماً با برچسب `[HYPOTHESIS]` و `[فرضیه نیازمند تست - مجهول رسمی]` مستند می‌شوند و در چک‌لیست اقدامات حقیقت‌یابی درج می‌گردند.

---

## ۷. استناد داده‌ها و برچسب‌های منشأ (Data Provenance Tags)

تمامی خروجی‌های مدون در اسناد ۵ لایه‌ای و کتابچه مستر، دارای شناسنامه منشأ دقیق هستند:

- `[FACT]`: حقایق قطعی، تاییدشده و ثبت‌شده توسط بنیان‌گذار.
- `[DECISION]`: مصوبات و انتخاب‌های راهبردی در ایستگاه‌های تصمیم‌گیری.
- `[FORMULA]`: روابط محاسباتی، نقطه سربه‌سر و شاخص‌های مالی اقتصاد واحد.
- `[BENCHMARK]`: استانداردها و کدهای طبقه‌بندی جامع ۷۵۳ صنف و ۳۱ صنعت کلان ایران.
- `[HYPOTHESIS]`: مجهولات ثبت‌شده، فرضیات نیازمند تست و ماموریت‌های میدانی.

---

## ۸. معماری رابط کاربری تک‌رنگ اجرایی (Monochrome Executive UI)

رابط کاربری از الگوهای مکالمه‌ای و چت‌بات‌ها پاک‌سازی شده و به ساختاری حرفه‌ای ارتقا یافته است:

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  HEADER: نشان سیستم  |  عنوان صنف انتخابی  |  ذخیره خودکار  |  دکمه اسناد   │
├─────────────────────────────────────────────────────────────────────────────┤
│  PHASE RAIL: فاز ۱ تا ۸ با شاخص‌های پیشرفت، قفل‌ها و تایید گیت‌ها           │
├──────────────────────────────┬──────────────────────────────────────────────┤
│  CONTEXT LEDGER (ستون راست): │  QUESTION CARD (ستون چپ):                    │
│  - اطلاعات هویتی و صنفی      │  - نشانگر فاز و گام جاری (۱ از ۵)            │
│  - حقایق استخراج‌شده [FACT]  │  - عنوان پرسش و چرایی اهمیت آن               │
│  - مجهولات دوسیه [HYPOTHESIS]│  - شبکه ۲×۲ گزینه‌ها با امکان انتخاب کیبورد  │
│  - تصمیم‌های مصوب [DECISION] │  - گزینه «پاسخ خودم را می‌نویسم» (اینلاین)   │
│                              │  - دکمه‌های «بازگشت» و «تایید و ادامه»       │
└──────────────────────────────┴──────────────────────────────────────────────┘
```

- **پالت رنگی:** پس‌زمینه سیاه مطلق (`#000000`)، سطوح تیره (`#080808`, `#0D0D0D`, `#111111`)، حاشیه‌ها با شفافیت سفید (`white/20`)، حالت انتخاب‌شده (`bg-white text-black`).
- **حذف حس چت‌بات:** فاقد حباب‌های پیام، آواتار بات، نقطه تایپینگ، داک دائمی ارسال پیام و انیمیشن‌های فانتزی/کانفتی.
- **انطباق ویوپورت:** ۱۰۰dvh بدون اسکرول پنجره در ۸ سایز استاندارد (موبایل ۳۶۰ تا مانیتور ۱۹۲۰).

---

## ۹. امنیت، محرمانگی و پالایش نشست (Security & Storage)

1. **مدیریت سشن BYOK:** کلید API فقط در `sessionStorage` ذخیره می‌شود و با بستن تب مرورگر محو می‌گردد. در صورت وجود کلیدهای قدیمی در `localStorage`، سیستم آن‌ها را فوراً پاک‌سازی و مهاجرت می‌دهد.
2. **پالایش آدرس سرور:** اجبار به پروتکل `https://`، مسدودسازی شبه‌پروتکل‌های اسکریپتی (`javascript:`, `data:`) و جلوگیری از تزریق اطلاعات کاربری.
3. **سانسور اسرار در لاگ‌ها:** حذف خودکار تمامی کلیدهای گوگل و توکن‌ها قبل از نمایش یا ثبت خطا با برچسب `[REDACTED]`.
4. **تاب‌آوری شبکه:** کنترل خطای ۴۲۹ با توقف نمایی (Exponential Backoff: 1s, 2s, 4s) و ترجمه خطاهای قطعی اینترنت به راهنمای شفاف فارسی.
5. **پالایش پایدارسازی داده‌ها:** متد `PersistenceManager` پیش از ذخیره‌سازی محلی یا برون‌بری JSON، کلیه اطلاعات حساس را حذف می‌کند.

---

## ۱۰. بهینه‌سازی بارگذاری، تکه‌بندی کد و کارایی (Performance)

با تنظیم تخصصی Rollup در `vite.config.js`:
- حجم باندل اصلی بیش از **۸۶٪ کاهش** یافت (از ۲.۱ مگابایت به ۲۹۷ کیلوبایت).
- داده‌های بزرگ ۷۵۳ صنف (`data-taxonomy-753`)، پایگاه دانش (`data-wiki-knowledge`)، واژگان (`data-vocabulary-map`) و قالب‌ها (`data-templates`) در چانک‌های مستقل ایزوله شدند.
- کلیه مودال‌ها (`DeliverableModal`, `SettingsModal`, `WikiModal`, `GuildSelectorModal`) با `React.lazy` و `Suspense` بارگذاری تنبل می‌شوند.
- سرعت جستجو در کاتالوگ ۷۵۳ صنف به زیر ۲۵ میلی‌ثانیه و صدور اسناد به کمتر از ۱ میلی‌ثانیه رسید.
