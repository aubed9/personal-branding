# گزارش ممیزی جامع امنیت و محرمانگی داده‌ها (SECURITY-AUDIT)
> **پروژه:** پلتفرم استراتژی برند «دیجیتال مارکت» (DIGITAL MARKET)  
> **دامنه ارزیابی:** امنیت کلیدهای اختصاصی (BYOK)، پالایش آدرس‌ها، محافظت از نشست، سانسور داده‌های حساس در لاگ‌ها و ایزولاسیون پایگاه داده محلی  
> **وضعیت نهایی:** تایید با درجه امنیتی ممتاز (CLEAN & SECURE - Zero Leakage)  

---

## ۱. ممیزی چرخه حیات کلیدهای هوش مصنوعی (Session-Only BYOK)

در نسخه‌های سنتی وب‌اپلیکیشن‌ها، کلیدهای اختصاصی هوش مصنوعی (مانند Google Gemini API Key) در `localStorage` ذخیره می‌شدند که خطر نشت توسط اسکریپت‌های مخرب یا دسترسی فیزیکی به مرورگر را در پی داشت.

### معماری امنیتی جدید در `endpointSecurity.js`:
- **ذخیره‌سازی منحصراً در `sessionStorage`:** کلید واردشده توسط کاربر صرفاً در حافظه تب جاری ذخیره شده و به محض بسته شدن تب، محو می‌گردد.
- **پاک‌سازی قهری کلیدهای قدیمی:** متد `SessionKeyManager.purgeLegacyKeys()` در لحظه آغاز به کار سامانه، تمامی کلیدهای موجود در `localStorage` را اسکن کرده، در صورت وجود به سشن منتقل نموده و کلید را از دیسک پاک می‌کند.
- **آزمون تجربی ماندگاری:** آزمون‌های مایل‌استون ۶ و ۸ تایید کردند که پس از بارگذاری مجدد، هیچ کلیدی در کلیدواژه‌های ذخیره‌سازی محلی دیسک باقی نمی‌ماند.

---

## ۲. اعتبارسنجی و پالایش سخت‌گیرانه آدرس سرور (Endpoint Sanitization)

کاربران مجاز به اتصال به گیت‌وی‌ها یا سرورهای واسط سفارشی خود هستند. جهت جلوگیری از حملات جعل درخواست (SSRF) و ربایش اعتبارنامه:

1. **اجبار به پروتکل HTTPS:** هرگونه آدرس خارجی که با `http://` آغاز شود فوراً رد شده و خطای امنیتی صادر می‌گردد (تنها استثنا: توسعه محلی روی `localhost` یا `127.0.0.1`).
2. **مسدودسازی شبه‌پروتکل‌های اسکریپتی:** وارد کردن آدرس‌های با پیشوند `javascript:`, `data:`, `file:`, `vbscript:` با خطای شدید امنیتی مسدود می‌شود.
3. **جلوگیری از تزریق اطلاعات کاربری (Userinfo Injection):** آدرس‌هایی که حاوی نام کاربری و رمز عبور در قالب `https://user:pass@evil.com` باشند بلافاصله مسدود می‌گردند تا از افشای توکن‌ها جلوگیری شود.

---

## ۳. سانسور خودکار اسرار در پیام‌ها و لاگ‌ها (Secret Redaction)

تابع `redactSensitiveData` در تمامی نقاط خروجی، لاگ‌های کنسول و پیام‌های خطای شبکه فعال است:
- کلیدهای استاندارد گوگل (`AIzaSy...`) با الگوی ریجکس شناسایی و با برچسب `[REDACTED_GOOGLE_KEY]` جایگزین می‌شوند.
- پارامترهای کوئری حاوی کلید (`?key=...`) به صورت خودکار به `?key=[REDACTED]` تغییر می‌یابند.
- کلید اختصاصی واردشده توسط کاربر نیز در صورت درج در متن خطاها به طور کامل با `[REDACTED_API_KEY]` جایگزین می‌شود.

---

## ۴. تاب‌آوری شبکه و مدیریت ترافیک (Network Resilience & Backoff)

سرویس `secureFetchJson` دارای لایه‌های دفاعی مستقل در برابر نوسانات اینترنت ایران و محدودیت‌های سرور است:
- **توقف نمایی در خطای نرخ درخواست (HTTP 429 Backoff):** در صورت دریافت خطای ۴۲۹، سامانه بلافاصله به کاربر خطا نمی‌دهد، بلکه با استراتژی Exponential Backoff (تاخیرهای ۱ ثانیه، ۲ ثانیه و ۴ ثانیه) درخواست را تکرار کرده و در صورت رفع محدودیت به مسیر ادامه می‌دهد.
- **تایم‌اوت سخت‌گیرانه ۳۰ ثانیه‌ای:** با استفاده از `AbortController` از مسدود شدن مرورگر بر اثر افت شبکه جلوگیری می‌شود.
- **ترجمه خطاهای قطعی اینترنت به راهنمای بومی:** در صورت بروز خطای تایپ `TypeError: Failed to fetch` (که ناشی از قطعی شبکه یا فیلترینگ است)، پیام خطای شفاف فارسی `ارتباط با اینترنت یا سرور برقرار نشد. لطفاً اتصال شبکه را بررسی نمایید.` به کاربر نمایش داده می‌شود.

---

## ۵. امنیت پایدارسازی داده‌ها و برون‌بری فایل (Data Persistence Hygiene)

موتور `PersistenceManager` تدابیر زیر را جهت حفظ محرمانگی داده‌ها اعمال می‌کند:
1. **جداسازی کامل پیکربندی از اطلاعات تجاری:** کلیدهای API و آدرس‌های سرور هرگز در شیء وضعیت پروژه ذخیره نمی‌شوند.
2. **پاک‌سازی در زمان Export:** در هنگام دانلود فایل خروجی پروژه (`exportStateAsJson`)، کلیه فیلدهای حساس پیش از تشکیل رشته JSON حذف می‌گردند.
3. **تاب‌آوری در برابر داده‌های مخدوش:** در صورتی که دیتای ذخیره‌شده در مرورگر به هر دلیلی مخدوش شود، سامانه کرش نمی‌کند؛ داده‌های مخدوش به کلید پشتیبان `dm_project_state_corrupt_backup` منتقل شده و سامانه با وضعیت پاک و گزارش امن بالا می‌آید.
